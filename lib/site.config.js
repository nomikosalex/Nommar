// Site-wide presentation config (was the DC component props).
export const CONFIG = {
  heroStyle: 'band', // 'centered' | 'split' | 'band'
  enableMotion: true,
  priceMode: 'from', // 'from' | 'onRequest' | 'dash'
};
